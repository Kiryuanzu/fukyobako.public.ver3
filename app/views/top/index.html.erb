<head>
 <%= render 'top/script' %>
</head>
<body>
  <% unless current_user %>
    <div class="notuser">
      <span class="notuser-text"><%= link_to "新規登録", new_registration_path(resource_name) %>or<%= link_to "ログイン", new_session_path(resource_name) %>して布教箱を利用する
      </span>
    </div>
  <% else %>
    <div class="contents">
       <%= render partial: 'odais/new' , odai: @odai %>
  <% end %>
    <%= render 'top/contents' %>
    </div>
</body>